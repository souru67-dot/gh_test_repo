# バックログ（Build 2 / v1.1）

思いついたもの・見つけたものは順不同で足していってください。整理はこちらでやります。

**行き先は2つあります。**

| | いつ出るか |
|---|---|
| **Build 2** | ベータ期間中。**v1.0 のまま**ビルドを差し替える |
| **v1.1** | リリース後。次のバージョンとして出す |

**凡例** — 効果: ユーザーへの効き目 / 手間: 実装コスト

| | 意味 |
|---|---|
| 🔧 | v1.0 の宿題（機能ではないが、放置すると効いてくる） |
| ✨ | 機能追加 |
| 💰 | 収益 |
| 🧹 | 技術的負債 |

---

## 🚦 振り分けの基準

| Build 2 に入れる | v1.1 に回す |
|---|---|
| **落ちる・データが壊れる** | 新機能 |
| **審査で落ちる** | 設計に触る変更 |
| **初見で誤解する・操作できない** | 画面の作り直し |
| **文言・ラベルの誤り**（1行で直る） | Android 側の追随 |

> **迷ったら v1.1 です。** ビルドを差し替えるたびに Beta App Review と実機の再確認が
> 発生し、リリースがその分遅れます。判断は好みではなく
> **「直さないと出せないか？」**で。出せるなら v1.1 です。

**締切の目安は ベータ Day 10**（`IOS_LAUNCH_GUIDE.md` §8 Step 6-A）。
それ以降に出たものは、落ちる系を除いて v1.1 へ回してリリースを優先します。

---

## 📦 Build 2 に入れるもの

| # | 内容 | 種別 | 規模 | 出所 | 状態 |
|---|---|---|---|---|---|
| — | （まだありません） | | | | |

**書き方**

- **種別**: 落ちる / 審査 / 誤解 / 文言 / その他
- **規模**: 小（1〜数行）/ 中（1画面で収まる）/ **大（設計に触る → v1.1 へ）**
- **出所**: 自分 / テスターの名前 / 審査 ＋ 日付
- **状態**: 未着手 / 対応中 / 済（コミットのハッシュ）

**記入例**

```
| B-1 | レイアウトのラベルを「自動 / 1列 / 2列」へ | 文言 | 小 | 自分 2026-08-05 | 未着手 |
| B-2 | 1万枚のライブラリで自動仕分け中に落ちる | 落ちる | 中 | 田中さん 2026-08-08 | 済 a1b2c3d |
```

### Build 2 を出す手順

1. Xcode で **Build 番号を +1**（Version は `1.0` のまま `2`, `3` …）
2. Archive → Upload
3. TestFlight → 外部グループに **手動でビルドを追加**（自動では入りません）
4. **「テスト内容」に何を直したか書く** ← 同じ報告の重複が減ります

> 2回目以降のビルドも Beta App Review の対象ですが、大きな変更がなければ
> 短時間で通ることが多いです。テスターの端末には自動で届きます。

---

## 🗂 v1.1 — あなたから届いたもの

以下はすべて**リリース後**に着手します。

### 1. ✨ HEXチップの値を自分で編集できるようにする

> 2026-08-04 受領

コラージュの HEXチップ（写真の左下に出る色ドット＋HEX）と、カラーパレット（採集票）に
印字される色を、自動判定に任せきりではなく**自分で決められる**ようにする。

**なぜ効くか**: このアプリの書き出し画像で、いちばん「作品」らしさを出しているのが
HEX の印字です。そこが1つでも意図と違う色だと、作品全体が惜しくなります。
自動判定は主役の色を狙いますが、「この写真で見せたいのはこの色」は撮った本人にしか
分かりません。**作品性を自分でコントロールできる**という点で、価値の大きい要望だと思います。

**実装メモ（設計時に読む）**

現状、写真の色情報は2つに分かれています。

| | 中身 | 使われる場所 |
|---|---|---|
| `dominantColor: Int32?` | 抽出された実際の色（HEX の元） | HEXチップ・パレット・タイルの色ドット |
| `bucketKey: String?` | 12色のどれか（分類） | ハントの絞り込み・カメラの一致判定 |

**⚠️ 今の `rebucket()` は `bucketKey` だけを書き換え、`dominantColor` は触りません**
（`AppState.swift:361`）。つまり「長押しで青→赤に変更」しても、パレットに印字される
HEX は青のままです。**この機能を作るなら、まずここを揃えるべきです。**

推奨する形:

```
ユーザーが HEX を編集
      ↓
dominantColor を更新
      ↓
ColorBridge.shared.classifyKey(colorInt:) で bucketKey を自動で引き直す
```

こうすると「色を選び直す」と「HEXを編集する」が1つの操作に統合され、
2つの値が食い違うことが構造的に無くなります。

**UI の候補**（要検討）
- 写真の長押しメニューに「色を編集」→ カラーピッカー（今日の色のホイールを流用）
- スポイト: 写真の上をドラッグして、その位置の実際のピクセル色を拾う
  ← **これが一番この app らしいと思います。** 「見せたい色」を写真から直接指せる
- HEX の直接入力（`#FF5A3C`）も欲しい人はいる

**効果: 大 / 手間: 中**

### 2. ✨ コラージュに文字を入れられるようにする

> 2026-08-05 受領 — 「チェキに文字を書くイメージ」

**評価: やる価値が高い。**

1. コラージュアプリで文字は**事実上の必須機能**。無いことが選ばれない理由になる
2. **チェキの発想が効いている。**「キャプションを付ける」ではなく「白帯に一言書く」
   だと、汎用の文字ツールとは別物になる。他アプリとの差はここに出る
3. **拡散に直結する。** 自分の言葉が入った画像は投稿されやすい

**土台はある。** 書き出しは `canvas(width:)` 1本で `scale = width / 360` を全オーバーレイ
に配っているので、文字も日付スタンプ・透かし・HEXチップと同じ仕組みに乗せられる。

**設計時に必ず読むこと**

| | |
|---|---|
| **座標は正規化（0〜1）で持つ** | ポイントで持つとプレビュー340pt→書き出し2160pxでズレる。`focals`（`CollageSettings`）が既にこの方式なので倣う |
| **フォントが機能の質を決める** | システムフォントだと「よくあるアプリ」になる。日本語の手書き系はライセンスとファイルサイズ（数MB〜）が課題。OFL 系（Klee One / Zen Kurenaido など）を検討 |
| **既存の焼き込みと衝突する** | 透かしは左下、日付は右下。`hexChipBottomInset` / `footerInset` と同じ発想で避ける仕組みが要る |
| **無料/Proの線引き** | 全部Proにすると拡散が鈍る。**無料で1つ、Proで複数＋フォント全部**が妥当 |

**決めどころ**: 「好きなところ」か「チェキの白帯」か。どこでも置けるほうが自由だが、
チェキらしさは白帯に限定したほうが出る。**既定は白帯・ドラッグでどこへでも**が折衷案。

**効果: 大 / 手間: 中〜大**

### 3. 🔧 レイアウトの「グリッド」と「2列」が同じ結果になる

> 2026-08-05 受領 — 「グリッドと2列って同じ？」

**実際に同一です。** `CollagePresets.kt:92` の `columnsFor`:

| 枚数 | グリッド | 2列 | |
|---|---|---|---|
| 2枚 | 2列 | 2列 | **同じ** |
| **4枚** | **2列** | **2列** | **同じ** |
| 3枚 | 3列 | 2列 | 違う |
| 6枚 / 9枚 | 3列 | 2列 | 違う |

**4枚はテンプレートの大半が使う枚数**なので、普通に触ると「同じ」に見えます。
効かないボタンは無いより悪い。

原因は「グリッド」が実際には**「列数を枚数から自動で決める」**モードだから。
4枚では自動が2列を選ぶので、明示的な2列と一致します。

**直し方（安い順）**

1. **ラベルを「自動 / 1列 / 2列」に変える** ← 推奨。表示文言だけでロジック変更なし。
   「自動」なら4枚で2列と一致しても筋が通る
2. **「3列」を足す。** 自動と別物になる場面が増え、選択肢としても意味が出る
3. 枚数に応じて選べない選択肢を無効化する ← 一番親切だが実装は重い

**効果: 中（迷いが消える） / 手間: 1 は小・2 は中**

---

## 🗂 v1.1 — こちらからの提案

### 🔧 A. `.xcodeproj` をリポジトリに入れる

**v1.1 の最初にやるべきです。** いま署名設定・Info.plist の権限文言・アイコンの
割り当ては**あなたの Mac の中にしか存在しません**。Mac が壊れると、ビルドできる状態を
一から作り直すことになります。

**効果: 保険 / 手間: 小**（`IOS_LAUNCH_GUIDE.md` §1 にも記載済み）

### 🔧 B. `rebucket` と `dominantColor` の不整合を直す

上の「1.」の前提になる修正。単体でも意味があります。

**効果: 中 / 手間: 小**

### 🔧 C. Android 版を iOS に追いつかせる

v1.0 の仕上げは iOS 側だけに入っていて、Android は取り残されています。
（下帯の配置・パレットの意匠・コラージュ設定の永続化・カメラの色一致判定）

Android を出す予定があるかどうかで優先度が変わります。**出さないなら、この項目は
落として構いません。**

**効果: Android を出すなら必須 / 手間: 大**

### ✨ D. ホーム画面ウィジェット「今日の色」

WidgetKit で、今日の色をホーム画面に出す。タップでカメラが開く。

**なぜ効くか**: このアプリの弱点は「開く理由が毎日は無い」ことです。ウィジェットは
**開かせるための機能**で、写真アプリの継続率にいちばん効く打ち手です。
今日の色は毎日変わるので、ウィジェットとの相性が非常に良い。

**効果: 大（継続率） / 手間: 中**

### ✨ E. 「今週集めた色」の共有カード

1週間ぶんのハントを、色のグリッド1枚にまとめて共有できるようにする。

**なぜ効くか**: いまの拡散導線はコラージュの透かしだけです。これは「作品を作った人」
しか流してくれません。集めるだけで作れるカードがあると、**コラージュを作らない層も
広告塔になります**。「今週の色」はSNSの定型フォーマットとしても回しやすい。

**効果: 大（拡散） / 手間: 中**

### ✨ F. コレクションの達成率・統計

12色それぞれの収集数、今月の推移、「まだ集めていない色」の提示。

**なぜ効くか**: 「集める」がテーマなのに、いま**集まった実感を返す画面がありません**。
埋まっていく手応えは、この種のアプリの中毒性の芯です。D の「開く理由」とも噛み合います。

**効果: 中〜大 / 手間: 中**

### ~~🔧 N-0. 共有キャプションが日本語でハードコードされている~~ → **v1.0 で対応済み**

`CollageView.swift` の共有キャプションが `Localizable.strings` を通っておらず、
英語・韓国語・簡体字のユーザーにも日本語のタグが出ていた。**2026-08-04 に4言語化**。

| | キャプション |
|---|---|
| ja | `ColorHuntで色あつめ 🎨📸 #カラーハント #色集め #組写 #colorhunt` |
| en | `Colour hunting with ColorHunt 🎨📸 #colorcollage #photodump #colorpalette #aesthetic` |
| ko | `ColorHunt로 색 모으기 🎨📸 #컬러헌트 #색모으기 #네컷사진 #포토덤프` |
| zh-Hans | `用 ColorHunt 收集颜色 🎨📸 #色彩收集 #拼贴 #照片墙 #调色板` |

> タグは育て方次第で変わるものなので、**反応を見て入れ替えてください**。
> `Localizable.strings` の1行を書き換えるだけです（アプリの更新は必要）。

### 💰 N. 英語圏でのブランド衝突を再評価する

**v1.0 では「ColorHunt のまま長い名前で通す」と決めました**（2026-08-04）。

**その理由**（判断の記録・蒸し返さないこと）:

1. カラーハント系の競合は、実際に触った印象として**強くない**
2. それよりも「**SNS で使われる言葉としての馴染みやすさ**」を取る。
   名前がそのままハッシュタグになる設計は既に実装済み（上の N-0）で、
   `#カラーハント` `#色集め` は競合しておらず、自分で育てられる

判断を覆す必要はありませんが、**リリース後に効き目を見る価値があります**。

- App Store には **`ColorHunt - Color Scavenger Hunt`**（ID 1202422081）が既にあり、
  「カメラで色を探す」というコンセプトが近い
- さらに英語の `#colorhunt` は **`colorhunt.co`**（デザイナー向けパレットサイト。
  Instagram `@color.hunt` 6.8万フォロワー）が押さえていて、辿ると写真ではなく
  **パレットの色見本**が並びます
- **どちらも日本語圏には効きません。** `#カラーハント` `#色集め` `#組写` は
  競合しておらず、自分で育てられます。**英語圏の検索とタグでのみ効いてきます**

**見るべき数字**: App Store Connect → 分析 → **検索キーワード経由のインストール**。
英語圏だけ極端に低ければ、これが原因の可能性があります。

**その場合の打ち手**（軽い順）
1. **英語のサブタイトル／キーワードを見直す。** 名前は変えず、`collage` `palette`
   `film` 側で拾う。既に `ColorHunt: Color Collage` と差別化済み
2. **英語ロケールの名前だけ変える。** App Store Connect は言語ごとに別名を付けられます。
   日本語のブランドを保ったまま英語圏だけ差し替えられる
3. 全面改名 ← レビューとランキングを捨てることになるので、最後の手段

> **改名がいちばん安いのは v1.0 提出前でした。** 今後は高くなる一方なので、
> やるなら「数字を見て、早めに」です。

**効果: 英語圏次第 / 手間: 1 は小・2 は中**

### ✨ O. 韓国語・簡体字のスクリーンショット

**英語版は v1.0 に入れました**（2026-08-05・7枚）。ここに残るのは**韓国語・簡体字**です。

- その国のストアにしか効かないため、**日本語での初動が落ち着いてからで十分**です
  （英語版と違い、他国のフォールバック先にはなりません）
- 見出しの対訳は `OWNER_TASKS.md` §D に揃っているので、残るのは
  **端末言語を切り替えて撮り直す＋見出しを乗せる**作業だけ
- N（英語圏のブランド衝突）と原因が重なる領域なので、**特定の言語圏が伸びないときは
  スクショを先に疑ってください**。名前より先に効きます

**効果: 韓国・中国語圏で中 / 手間: 中（1言語あたり半日）**

### ✨ G. 繁体字（zh-Hant）の追加

台湾・香港はこの系統のカメラ／コラージュアプリが特に強い市場です。
簡体字があるので、翻訳コストは低め（用語は要調整）。

**効果: 中 / 手間: 小**

### 💰 H. 季節テンプレートパック（¥250〜300）

桜・夏祭り・紅葉・クリスマス。既存の Pro 購入者にも売れるので、**買い切りの弱点を
そのまま埋められます**。プライバシー表示は無傷、SDK 依存もゼロ。

`IOS_LAUNCH_GUIDE.md` §6 で「リワード広告より先に試すべき」と結論を出した施策です。

**効果: 中（継続収益） / 手間: 中**

### 💰 I. リワード広告（v1.1 で再評価）

v1.0 では見送りました。「データを収集していません」バッジを失う代償が大きく、
**一度手放すと戻せない**ためです。Pro の実転換率が見えてから判断します。

判断材料が揃うのは、リリースから最低3ヶ月後。詳細は `IOS_LAUNCH_GUIDE.md` §6。

**効果: 不明（要データ） / 手間: 中**

### ✨ J. App Preview 動画

`REEL_SCRIPT.md` に24秒・7カットの台本があります。ストアの1枚目が動画になると
コンバージョンが上がります。

**効果: 中 / 手間: 小（台本済み）**

### 🧹 K. `CollageView.swift` の分割

2,100行あり、1ファイルとしては限界を超えています。テンプレ定義・レンダラー・
設定UI の3つに割るのが自然です。

**効果: 開発速度 / 手間: 中**

### 🧹 L. Swift のテストを入れる

現在テストがゼロです。最低限、色分類（`DominantColor` / `ColorBridge`）と
コラージュ幾何（`CollageGeometry`）は、壊れたら気づける状態にしたい。

**効果: 保険 / 手間: 中**

### 🧹 M. 写真アクセス「一部のみ」への対応

現状、資産IDで照合できないためピクセルハッシュに頼っており、この経路の重複を
防げません（`IOS_LAUNCH_GUIDE.md` §3.1 に既知の制限として記載）。

**効果: 小（該当ユーザーには中） / 手間: 中**

---

## 決めるとき用のメモ

v1.1 に全部は入りません。**リリース後の実データで選ぶ**のが正解です。

- **継続率が低かった** → D（ウィジェット）・F（統計）
- **ダウンロードが伸びない** → E（共有カード）・J（動画）・G（繁体字）
- **使われているが売れない** → H（テンプレパック）。ペイウォールの見直し
- **落ちる／遅いの声** → M・L を先に

> リリース直後にやることは、機能追加ではなく**レビューを読むこと**です。
> 上のリストより、最初の20件のレビューのほうが正確に次を教えてくれます。
